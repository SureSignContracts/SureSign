// SureSign's public version history starts here, at V1.0.0 — the first
// officially deployed production release. A version represents a released
// product, not development progress — do not bump this for ongoing feature
// work, fixes, or improvements. Only change APP_VERSION when a new version
// is intentionally, officially published — see CLAUDE.md (Versioning &
// Release Notes Policy) for the full rules.
export const APP_VERSION = 'V1.0.0';
export const APP_VERSION_LABEL = `SureSign ${APP_VERSION}`;
export const APP_VERSION_DATE = 'July 2026';
export const APP_VERSION_STATUS = 'Current Release';
